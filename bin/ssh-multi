HOSTS=$(cat $1)

for host in $HOSTS; do
    tmux split-window -h "ssh -l root $host"
    tmux select-layout tiled > /dev/null
done

tmux set-window-option synchronize-panes on > /dev/null
tmux kill-pane -t 1
